name: "Kofin Sync Queue"
guid: "eed3be84-d5cc-4f3b-b346-ba80c81cbec9"
# The PRIMARY ABI row. One source tree is built against several Jellyfin ABIs
# (the matrix is in .github/workflows/abis.yml); these three describe the lowest
# one, and are what a bare `tools/package.sh` produces. Every other row
# overrides them through the environment.
#
# version tracks the Jellyfin ABI it is built against: <server major.minor>.0.<build>.
# The <build> component is shared by every row, so the same source ships as
# 10.11.0.N and 12.0.0.N and a v12 server ranks the v12 zip highest — the only
# thing stopping it installing the 10.11 one, since Jellyfin treats targetAbi as
# a floor (jellyfin#11331, closed Not A Bug).
#
# The authority is the csproj's PluginVersion, which is what tools/package.sh
# reads to name the zip and stamp meta.json; the value here is documentation and
# is asserted against it at release time.
version: "10.11.0.2"
targetAbi: "10.11.0.0"
framework: "net9.0"
owner: "kontell"
overview: "Typed change queue for the Kofin Kodi client"
description: "Companion plugin for plugin.video.kofin: records library and user-data changes as typed records (change reason, series/season parents), serves them with query-time Etags and an in-band retention cutoff, so offline Kodi boxes catch up with minimal traffic."
category: "General"
# The logo, in the two places Jellyfin looks for one.
#
#   image     a file inside the zip, written to meta.json as "imagePath".
#             Jellyfin resolves it against the *installed plugin folder* and
#             serves it from /Plugins/{guid}/{version}/Image, which is what the
#             Dashboard -> Plugins tile of an installed plugin shows. The
#             server refuses a path that escapes the plugin folder, so this is
#             a bare filename.
#   imageUrl  the copy the *catalogue* shows, before anything is installed:
#             repository.kontell's generate_jellyfin_repo.py copies it out of
#             meta.json into manifest.json, where it has been reading "" until
#             now. It has to be reachable by a server that does not have the
#             plugin yet, so it cannot come out of the zip.
image: "logo.png"
imageUrl: "https://raw.githubusercontent.com/kontell/jellyfin-plugin-kofinsyncqueue/main/logo.png"
artifacts:
  - "Jellyfin.Plugin.KofinSyncQueue.dll"
  - "LiteDB.dll"
changelog: |-
  - Built for Jellyfin 12.0 as well as 10.11, from one source tree: the release
    carries a zip per server line, each declaring its own targetAbi, and a
    server installs the newest one it can run.
  - The plugin has a logo. The catalogue entry and the installed plugin's tile
    show the Kofin mark instead of a blank placeholder.
